{
  "name": "capstone-report-generator",
  "private": true,
  "scripts": {
    "build": "if [ -d doc-generator ]; then cd doc-generator && npm install && npm run build; else npm install && npm run build; fi"
  }
}
